using System;

class PrintArray
{
    static void Main()
    {
        char[] merkit = { 'O', 'p', 'i', ' ', 'C', '#', '!'};

        foreach(char i in merkit)
        {
            Console.Write(i);
        }
    }
}